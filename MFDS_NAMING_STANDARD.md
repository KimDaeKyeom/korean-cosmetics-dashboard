# MFDS 화장품 원료 명칭 기준

- 기준 스냅샷: `20260806`
- 식약처 API 원료 행: **21,863건**
- 서로 다른 식약처 한글 표준명: **21,863개**

## 기준

1. `INGR_KOR_NAME` 원문을 **공식 한글 표준명(canonical)** 으로 사용한다. 표시용 명칭은 임의 번역·띄어쓰기 교정·구명칭 치환을 하지 않는다.
2. `INGR_ENG_NAME`은 영문 보조명이며 한글 표준명을 대체하지 않는다.
3. `INGR_SYNONYM`은 검색·매칭용 별칭으로만 사용한다. 별칭을 공식 표준명으로 출력하지 않는다.
4. CAS 번호는 동정 근거다. CAS 없음, 복수 CAS, 혼합물 가능성이 있어 명칭의 단독 기본키로 사용하지 않는다.
5. 비교 전 Unicode NFKC, 공백 축약, 유사 하이픈 통일을 적용한 `strict_key`를 사용한다. 원문 표시는 변형하지 않는다.
6. `compact_key`는 공백만 제거한 보조키다. 한 원료에 유일할 때만 자동 후보로 사용하고 충돌 시 수동 검수한다.
7. 상품 전성분 매칭 순서는 `한글 canonical strict → MFDS synonym strict → 영문 strict → unique compact`다.
8. 여러 MFDS 행에 매칭되는 명칭, 용량·옵션·구성 범위가 다른 명칭, OCR 추정치는 자동 승인하지 않는다.
9. 판매페이지의 관찰 명칭은 별도 `observed_name`으로 보존하고, 매칭된 식약처 `ingredient_id`와 함께 기록한다.
10. 식약처 스냅샷이 변경되면 기존 상품 매핑을 덮어쓰지 않고 기준 버전을 올려 재검증한다.

## 자동 승인 금지

- strict/compact key가 여러 `ingredient_id`로 충돌
- 한글 표준명 없이 영문명·CAS만 존재
- 동의어만 일치하고 원료 정의·CAS가 상충
- 혼합원료·착향제·색소 묶음·복합 추출물의 구성 경계 불명확
